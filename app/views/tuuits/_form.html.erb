<%= form_with(model: tuuit) do |form| %>
  <% if tuuit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tuuit.errors.count, "error") %> prohibited this tuuit from being saved:</h2>

      <ul>
        <% tuuit.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :id %>
    <%= form.number_field :id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :time %>
    <%= form.datetime_select :time %>
  </div>

  <div class="field">
    <%= form.label :tuuit %>
    <%= form.text_area :tuuit %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
